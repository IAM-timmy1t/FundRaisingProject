// Re-export the supabase client for consistency
export { supabase } from './customSupabaseClient';

// Also export from the cached client if needed
export { cachedSupabase } from './cachedSupabaseClient'; 